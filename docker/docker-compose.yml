services:
  sagebase:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    volumes:
      - ../src:/app/src
      - ../tests:/app/tests
      - ../database:/app/database
      - ../scripts:/app/scripts
      - ../baml_client:/app/baml_client # BAML生成コード
      - ../baml_src:/app/baml_src # BAML定義ファイル
      - ../.streamlit:/app/.streamlit # Streamlit認証設定をマウント
      - ../pyproject.toml:/app/pyproject.toml
      - ../pyrightconfig.json:/app/pyrightconfig.json
      - ../uv.lock:/app/uv.lock
      - ../README.md:/app/README.md
      - ~/.config/gcloud:/root/.config/gcloud:ro # GCP認証情報をマウント
    env_file:
      - ../.env
    environment:
      - ENV_VAR_NAME=env_var_value # 必要な環境変数を設定
      - DATABASE_URL=postgresql://sagebase_user:sagebase_password@postgres:5432/sagebase_db
      - GOOGLE_APPLICATION_CREDENTIALS=/root/.config/gcloud/application_default_credentials.json
      - GOOGLE_CLOUD_PROJECT=trust-chain-828ad
    ports:
      - "8000:8000" # 必要に応じてポートを設定
      - "8501:8501" # Streamlit用ポート
    stdin_open: true # 追加
    tty: true # 追加
    depends_on:
      - postgres

  sagebase-monitoring:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    volumes:
      - ../src:/app/src
      - ../tests:/app/tests
      - ../database:/app/database
      - ../scripts:/app/scripts
      - ../baml_client:/app/baml_client # BAML生成コード
      - ../baml_src:/app/baml_src # BAML定義ファイル
      - ../.streamlit:/app/.streamlit # Streamlit認証設定をマウント
      - ../pyproject.toml:/app/pyproject.toml
      - ../pyrightconfig.json:/app/pyrightconfig.json
      - ../uv.lock:/app/uv.lock
      - ../README.md:/app/README.md
      - ~/.config/gcloud:/root/.config/gcloud:ro # GCP認証情報をマウント
    env_file:
      - ../.env
    environment:
      - ENV_VAR_NAME=env_var_value # 必要な環境変数を設定
      - DATABASE_URL=postgresql://sagebase_user:sagebase_password@postgres:5432/sagebase_db
      - GOOGLE_APPLICATION_CREDENTIALS=/root/.config/gcloud/application_default_credentials.json
      - GOOGLE_CLOUD_PROJECT=trust-chain-828ad
    ports:
      - "8502:8502" # 監視用Streamlitポート
    stdin_open: true
    tty: true
    depends_on:
      - postgres
    command:
      [
        "uv",
        "run",
        "streamlit",
        "run",
        "src/monitoring_app.py",
        "--server.port=8502",
        "--server.address=0.0.0.0",
      ]

  bi-dashboard:
    build:
      context: ../
      dockerfile: src/interfaces/bi_dashboard/Dockerfile
    container_name: sagebase-bi-dashboard
    volumes:
      - ../src:/app/src
      - ../pyproject.toml:/app/pyproject.toml
      - ../uv.lock:/app/uv.lock
      - ../README.md:/app/README.md
    environment:
      - DATABASE_URL=postgresql://sagebase_user:sagebase_password@postgres:5432/sagebase_db
      - PYTHONPATH=/app
    ports:
      - "8050:8050" # BI Dashboard用ポート
    depends_on:
      - postgres
    stdin_open: true
    tty: true

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: sagebase_db
      POSTGRES_USER: sagebase_user
      POSTGRES_PASSWORD: sagebase_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../database/init.sql:/docker-entrypoint-initdb.d/01_init.sql:ro
      - ../database/02_run_migrations.sql:/docker-entrypoint-initdb.d/02_run_migrations.sql:ro
      - ../database/migrations:/docker-entrypoint-initdb.d/02_migrations:ro
      - ../database/seed_governing_bodies_generated.sql:/docker-entrypoint-initdb.d/03_seed_governing_bodies_generated.sql:ro
      - ../database/seed_political_parties_generated.sql:/docker-entrypoint-initdb.d/04_seed_political_parties_generated.sql:ro
      - ../database/seed_conferences_generated.sql:/docker-entrypoint-initdb.d/05_seed_conferences_generated.sql:ro
      - ../database/seed_parliamentary_groups_generated.sql:/docker-entrypoint-initdb.d/06_seed_parliamentary_groups_generated.sql:ro
      - ../database/seed_meetings_generated.sql:/docker-entrypoint-initdb.d/07_seed_meetings_generated.sql:ro
      - ../database/seed_politicians_generated.sql:/docker-entrypoint-initdb.d/08_seed_politicians_generated.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sagebase_user -d sagebase_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
